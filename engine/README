Install the following to get the game working:

    liballegro2.4
    liballegro2.4-dev

----

TODO list:	Add the documentation;
			Fix[Upgrade] the game "add element" in screen  action confirmation (doing 04/jun/2013);
			Upgrade the project makefiles;
			Plan the scenerey data storage;
			Create a "load scenery" function;
			Create the "controller" module;
			Create a game map editor, maybe based on the previous created map editor (not commited).

----

 System reference~

main modules folders:

	./system # gameSystem module
	./video  # gameVideo  module
	./brain  # gameBrain  module

auxiliary folders:

	./util    # utilitary headers
	./modules # extern modules (almost all)
	./libs    # extern libs

----
